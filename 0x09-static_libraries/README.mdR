C - Static libraries

Create the static library libmy.a containing all the functions listed below:

* Converting all .c files to .o files with "gcc -c *.c"

* Moving all the .o files to the libmy library with "ar rcs libmy.a *.o"

* and creating the library with "ar rcs libmy.a"

