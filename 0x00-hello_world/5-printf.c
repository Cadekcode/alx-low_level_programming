#include <stdio.h>

/**
 * main - a C program that prints exactly with proper grammar
 *
 * Return: Always Zero to be successful
 *
 */
int main(void)
{ 
	printf("w/ith proper grammar, but the outcome is a piece of art,\n")
	return (0)
}
